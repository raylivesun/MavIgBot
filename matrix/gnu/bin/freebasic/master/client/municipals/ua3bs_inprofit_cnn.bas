#lang "fblite"
#if 0
declare function ua3bs_inprofit_cnn (byref names as string, byref value as string, byref kits as string) as string 

type ua3bs_inprofit_ alias "ua3bs_inprofit"
     names as string
     value as string
     kits as string
     proc(ua3bs_inprofit_alias)
     end proc
     proc(ua3bs_inprofit_alias)
     names = names & "," & value & "," & kits
     end proc
     proc(ua3bs_inprofit_alias)
     return names
     end proc
     proc(ua3bs_inprofit_alias)
     return value
     end proc
     proc(ua3bs_inprofit_alias)
     return kits
     end proc
     proc(ua3bs_inprofit_alias)
     names = names & "," & value & "," & kits
     end proc
     proc(ua3bs_inprofit_alias)
     return names
     end proc
     proc(ua3bs_inprofit_alias)
     return value
     end proc
     proc(ua3bs_inprofit_alias)
     return kits
     end proc
     proc(ua3bs_inprofit_alias)
     names = names & "," & value & "," & kits
     end proc
     proc(ua3bs_inprofit_alias)
     return names
     end proc
     proc(ua3bs_inprofit_alias)
     return value
     end proc
     proc(ua3bs_inprofit_alias)
     return kits
     end proc
     proc(ua3bs_inprofit_alias)
     names = names & "," & value & "," & kits
     end proc
     proc(ua3bs_inprofit_alias)
     return names
     end proc
     proc(ua3bs_inprofit_alias)
     return value
     end proc
     proc(ua3bs_inprofit_alias)
     return kits
     end proc
     proc(ua3bs_inprofit_alias)
     names = names & "," & value & "," & kits
     end proc
     proc(ua3bs_inprofit_alias)
     return names
     end proc
     proc(ua3bs_inprofit_alias)
     return value
     end proc
     proc(ua3bs_inprofit_alias)
     return kits
     end proc
     proc(ua3bs_inprofit_alias)
     names = names & "," & value & "," & kits
     end proc
     proc(ua3bs_inprofit_alias)
     return names
     end proc
end type 


func main() as string
    local names as ua3bs_inprofit_alias
    local value as string
    local kits as string

    names.proc(ua3bs_inprofit_alias)
    value = names.proc(ua3bs_inprofit_alias)
    kits = names.proc(ua3bs_inprofit_alias)

    return names.proc(ua3bs_inprofit_alias) & "," & value & ","
end func
print main()
end declare
#endif 

